[project]
name = "worker"
version = "0.1.0"
description = "Async worker harness shell"
readme = "README.md"
authors = [
    { name = "copilot-swe-agent[bot]", email = "198982749+Copilot@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.11.9",
    "redis>=6.4.0",
]

[project.scripts]
worker = "worker:main"

[build-system]
requires = ["uv_build>=0.12.19,<0.13.0"]
build-backend = "uv_build"
